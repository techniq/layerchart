---
'layerchart': minor
---

feat(Chart): Infer stacking — `seriesLayout` now defaults to `'auto'`, long data stacks by the `c` categories its rows carry, and each mark decides whether it joins, so lines, points, interval values and hand-grouped marks are left as drawn.
