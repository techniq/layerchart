---
'layerchart': minor
---

[LinearGradient] Replace `from`/`to` with `stops` prop, which both simplifies tailwind color usage, and simplifies more complex cases (gradient encoding). Also support passing tuple values for explicit offsets (gradient threshold)
