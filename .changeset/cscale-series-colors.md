---
'layerchart': minor
---

feat(Chart): Default the color scale to the colors declared on `series`, so `<Spline stroke="species" />` matches its legend swatch without restating the palette as `cDomain`/`cRange`
