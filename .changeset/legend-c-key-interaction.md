---
'layerchart': minor
---

feat(Chart): Fade on hover and filter on click for a `legend` whose items name `c` categories rather than series, so a chart colored by `c` alone interacts the way configured series do. Adds `context.cKey(d)` — a row's category, beside `cGet`'s color.
