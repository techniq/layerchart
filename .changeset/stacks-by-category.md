---
'layerchart': minor
---

feat(Bar|Bars|Area|Waffle|Points|Spline): Draw the stack without a `seriesKey` when an ordinal `c` names the layers, so long data stacks from a composable `<Chart>` as it already does from `BarChart`.
