---
'layerchart': patch
---

fix(AreaChart|BarChart|LineChart|ScatterChart): Hide labels and points when toggling selected series from legend. . Add `getLabelsProps` and `getPointsProps` slot props, useful when override slot
