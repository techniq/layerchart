---
'layerchart': minor
---

feat(Chart): Make tooltips, `Highlight`, and brushing facet-aware — the pointer resolves the panel it's in and finds rows within it, while the crosshair and brush selection still span every panel.
