---
'layerchart': patch
---

fix(Tooltip): Center `x="data"` / `y="data"` on the interval a value falls in when the chart sets `xInterval` / `yInterval`, matching where the bar is drawn instead of sitting on its leading edge
