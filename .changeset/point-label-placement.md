---
'layerchart': minor
---

feat(utils): Add `getPointLabelLayout()` / `getPointLabelRect()` to resolve a point label's placement and bounding box (`smart` or discrete) — the shared geometry behind `<AnnotationPoint>` and `<Labels layout="voronoi" links>`, and a convenient `bounds` for `occlude()`.
