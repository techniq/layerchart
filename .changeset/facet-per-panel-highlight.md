---
'layerchart': minor
---

feat(Highlight|Tooltip): Add `facetAll` to mark and label the hovered position in every facet panel — each resolving its own row — plus `facet.rowAt()` and a `data` prop on `Tooltip.Root`.
